/**
 * Shared hook for HTTP/controllers (parity with WordPress `CCLPLUGINCore\BaseController`).
 * Grow this when multiple modules need the same REST helpers — keep thin.
 */
export class BaseController {}
