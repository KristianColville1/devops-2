# Copy to `.env` in this directory — docker compose auto-loads it.

# Shared secret — flows into node-backend env and baked into the frontend build.
DASHBOARD_TOKEN=dev-token

# Optional port overrides
# FRONTEND_PORT=80
